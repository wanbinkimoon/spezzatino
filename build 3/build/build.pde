int stageW      = 800;
int stageH      = 800;
color bgC       = #000000;
String dataPATH = "../../data";

// ================================================================

void settings(){ 
	size(stageW, stageH);

}

// ================================================================

void setup() {
	background(bgC);

}

// ================================================================

float xoff = 0.01;

// ================================================================
void draw() {
	background(bgC);
	noStroke();
	
	// ================================================================

	// float x = random(width);
	float x = map(noise(xoff), 0, 1, 0, width);
	xoff += 0.01;
// ================================================================

	ellipse(x, height / 2, 24, 24);
}
